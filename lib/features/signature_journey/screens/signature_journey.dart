import 'package:flutter/material.dart';

class SignatureJourney extends StatelessWidget {
  static const String routeName = '/signature_journey';
  const SignatureJourney({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('SignatureJourney')));
  }
}
